import type { Metadata } from 'next';
import Link from 'next/link';
import './globals.css';

export const metadata: Metadata = {
	title: 'Create Next App',
	description: 'Generated by create next app',
};

export default function RootLayout({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) {
	return (
		<html lang='en'>
			<body className='relative'>
				<nav className='absolute z-20 space-x-3 text-blue-500'>
					<Link className='hover:underline' href='/'>
						Face Landmarks
					</Link>
					<Link className='hover:underline' href='/hand-landmarks'>
						Hand Landmarks
					</Link>
					<Link className='hover:underline' href='/hand-brightness'>
						Hand Brightness
					</Link>
				</nav>
				{children}
			</body>
		</html>
	);
}
